name: First Workflow

on: [push]

jobs:
    run-shell-commands:
        runs-on: Ubuntu-latest
        steps:
            - name: echo hello world
              run: echo "Hello world"
            - name: Multiline Command Line
              run: |
                pwd
                date